using Open.FileSystemAsync;
using Open.GooglePhotos;

namespace Open.FileExplorer
{
    public class GooglePhotosPerson : FileSystemPerson
    {
        public GooglePhotosPerson(ContributorInfo metadata)
        {
            //Id = metadata.User;
            //Name = metadata.Nickname;
            //Thumbnail = metadata.Thumbnail;
        }
    }
}
